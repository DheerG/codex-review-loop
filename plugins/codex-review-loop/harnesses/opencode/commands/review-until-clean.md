---
description: Independently review the complete Git scope and repair it until clean
agent: build
---

Load the `review-until-clean` skill with the skill tool and follow it exactly.

Treat these arguments as outcome, base-ref, provider, or plan-mode hints:

$ARGUMENTS

If the current agent is read-only, add the skill's required execution step to the plan instead of claiming to run it.
